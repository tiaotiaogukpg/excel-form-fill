# 需要 Python 3.11+（browser-use 要求）

# Excel 读取
pandas>=2.0
openpyxl>=3.1

# 环境变量
python-dotenv>=1.0

# browser-use 填表（含 DeepSeek/LLM、CDP 浏览器）
# 安装时会自动拉取 openai、httpx、bubus、cdp-use 等依赖
browser-use>=0.11

# 可选：Playwright Chromium 可改善 CDP 兼容性
# playwright>=1.40
